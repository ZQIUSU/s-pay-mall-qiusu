<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="site.zqiusu.dao.IUserDao">

    <resultMap id="dataMap" type="site.zqiusu.domain.po.UserAccount">
        <id column="id" property="id"/>
        <result column="phone" property="phone"/>
        <result column="nickname" property="nickname"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" parameterType="site.zqiusu.domain.po.UserAccount" useGeneratedKeys="true" keyProperty="id">
        insert into user_account(phone, nickname, status, create_time, update_time)
        values(#{phone}, #{nickname}, #{status}, now(), now())
    </insert>

    <select id="queryByPhone" parameterType="java.lang.String" resultMap="dataMap">
        select id, phone, nickname, status, create_time, update_time
        from user_account
        where phone = #{phone}
    </select>

    <select id="queryByUserId" parameterType="java.lang.Long" resultMap="dataMap">
        select id, phone, nickname, status, create_time, update_time
        from user_account
        where id = #{id}
    </select>

</mapper>
